export interface queryData {
    q: string,
    type: string | null,
    limit: string | null,
}

export interface queryDataRepo {
    q: string,
    type: string | null,
    limit: string | null,
}

export interface queryDataService {
    q: string,
    type: string | null,
    limit: string | null
}